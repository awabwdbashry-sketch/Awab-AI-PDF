# Copy this file to ".env" for LOCAL development only.
# On Railway, do NOT upload a .env file - instead set this same variable
# under your Railway service's "Variables" tab. Railway injects it into
# the container's environment directly; the app reads it with
# os.getenv("GOOGLE_API_KEY") either way.

GOOGLE_API_KEY=your_google_api_key_here
